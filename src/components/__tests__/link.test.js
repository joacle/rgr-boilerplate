import React from 'react';
import { shallow } from 'enzyme';

import Link from '../Link';

test('adds 1 + 2 to equal 3', () => {
  expect(1 + 2).toBe(3);
});

// test('Link exists', () => {
//   const wrapper = shallow(<Link />);
//   expect(wrapper.find(Link)).toBe.have.length(1);
// });

